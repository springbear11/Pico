# PicoATE Station Config JSON

日期：2026-06-26

Station config 用来描述“这个工站有哪些设备，以及这些设备真实地址是什么”。

它不描述测试流程。

测试流程仍然放在 sequence JSON。

## 1. 为什么需要 Station Config

Sequence JSON 应该写逻辑设备 ID：

```json
{
  "dmm": "DMM1"
}
```

不要把真实 VISA 地址直接写进测试流程：

```json
{
  "visaAddress": "USB0::0x0957::0x0607::MY59001234::INSTR"
}
```

真实地址属于工站，不属于测试流程。

这样换工站、换仪器、换地址时，不需要改 sequence。

## 2. 文件结构

示例见：

```text
examples/stations/basic_station.json
```

基本格式：

```json
{
  "stationId": "bench-01",
  "name": "PicoATE Bench 01",
  "stopOnFailure": true,
  "scanDialogEnabled": true,
  "txtLogEnabled": true,
  "csvReportEnabled": true,
  "xlsxReportEnabled": true,
  "reportOutputDirectory": "",
  "snLength": 12,
  "metadata": {
    "site": "lab"
  },
  "devices": [
    {
      "deviceId": "DMM1",
      "deviceType": "DMM",
      "driverId": "fake.dmm",
      "address": "${DMM1_ADDRESS}",
      "lifetime": "Station",
      "options": {
        "defaultFunction": "DCV",
        "nplc": 10
      }
    }
  ]
}
```

## 3. 顶层字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `stationId` / `id` | string | 否 | 工站 ID。 |
| `name` | string | 否 | 工站显示名。 |
| `stopOnFailure` | bool | 否 | 默认 `true`。这是工站统一失败策略，同时作用于普通 Step、TestItem 子步骤、父 TestItem 和 Loop 的最终结果。 |
| `scanDialogEnabled` | bool | 否 | 默认 `true`。决定 TEST 模式是否自动弹出扫码框。 |
| `txtLogEnabled` | bool | 否 | 默认 `false`。打开后把界面实时执行日志同步写入 TXT。 |
| `csvReportEnabled` | bool | 否 | 默认 `false`。打开后生成测试结果 CSV。 |
| `xlsxReportEnabled` | bool | 否 | 默认 `false`。打开后生成带表头、筛选、列宽和结果颜色的 XLSX。 |
| `reportOutputDirectory` | string | 否 | 报告根目录。留空时使用 `PicoATE.UI.exe` 同目录下的 `log`。相对路径以 Station 文件目录为基准。 |
| `snLength` | number | 否 | SN 固定字符数，范围 0-256；`0` 表示不限制长度。 |
| `metadata` | object | 否 | 工站元数据。 |
| `devices` | array | 是 | 设备列表。 |

### 3.1 报告输出目录

TXT、CSV 和 XLSX 采用同一套文件名与分类规则：

```text
log/
  20260719/
    PASS/
      SN001.txt
      SN001.csv
      SN001.xlsx
    FAIL/
      SN002.txt
      SN002.csv
      SN002.xlsx
    SN003.txt       # 测试进行中或运行中关闭程序
    SN003.csv
```

- 有扫码 SN 时用 SN 命名；没有 SN 时用当前时间命名。
- 测试开始后立即在日期目录生成 TXT/CSV，TXT 随界面日志实时刷新；XLSX 在测试形成最终结果时生成。
- 最终 Pass 后移动到 `PASS`，最终 Fail、Error、Timeout、Stop 或 Abort 后移动到 `FAIL`。
- 测试中关闭 ATE 工具时不移动也不删除，文件保留在日期目录根部，表示这是一次未完成运行。
- 同名文件已存在时自动增加 `_2`、`_3`，不会覆盖历史文件。

### 3.2 统一失败策略

`stopOnFailure` 是运行时总开关，UI 和 CLI 都由 Core 执行同一套规则：

| 配置 | Failed / Error / Timeout 的处理 |
|------|---------------------------------|
| `true` | 当前节点 Retry 用尽后停止本 UUT；TestItem 跳过剩余子步骤，随后执行 Cleanup。 |
| `false` | 当前节点 Retry 用尽后继续；TestItem 继续执行剩余子步骤，父项汇总最严重结果，外层流程也继续。 |

- Retry 永远先执行，只有最终一次仍未通过时才应用工站策略。
- “继续”只改变流程是否推进，不会把失败结果改成通过；最终报告和良率仍按真实结果计算。
- 后续步骤若引用失败步骤的输出，会运行并得到明确的变量解析 `Error`；再后面的无关步骤仍可继续。
- 用户 Stop、Abort、Cancelled 不受“失败继续”影响，始终停止。
- While 的 `iterationErrorPolicy` 先处理循环内部本轮行为，Loop 最终结果再交给工站策略。
- Sequence 里旧的 `errorPolicy` 字段仍可读取；加载了 Station 配置后，工站策略覆盖其中的停止/继续选择。

默认 `StationSystem.json` 预留 `CAN1/CAN2`、`PPS1/PPS2`、`DMM1/DMM2`、
`SCOPE1/SCOPE2` 八个常用逻辑设备。它们默认 `enabled=false`，驱动和地址为空，
因此不会连接硬件，也不会触发缺少驱动的校验。项目使用某个槽位时，填写插件型号、
地址和选项后再明确启用。已有工站文件不会在打开时被自动补齐或覆盖。

## 4. Device 字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `deviceId` / `id` | string | 是 | 逻辑设备 ID，例如 `DMM1`、`CAN1`、`PSU1`。 |
| `deviceType` / `type` | string | 否 | 设备类型，例如 `DMM`、`CAN`、`PSU`。 |
| `driverId` / `driver` | string | 是 | 设备 session factory ID，例如 `keysight.34465a.visa`。 |
| `address` / `visaAddress` | string | 否 | 真实地址，例如 VISA 地址、IP、串口名、DLL 配置路径。 |
| `lifetime` | string | 否 | `Step`、`Run`、`Station`，默认 `Station`。 |
| `options` | object | 否 | driver 专属配置。 |
| `enabled` | bool | 否 | 默认 `true`；`false` 时忽略该设备。 |

## 5. Lifetime

| 值 | 意思 | 适合 |
|----|------|------|
| `Step` | 每次 step 自己打开/关闭 | 纯算法、一次性脚本、便宜无状态动作。 |
| `Run` | 一次运行期间保持连接 | 当前 sequence 独占的设备。 |
| `Station` | 工站级连接，长期保持 | DMM、CAN、电源、夹具、扫码枪等固定设备。 |

大小写、下划线、短横线、空格会被宽松处理。

例如：

```text
station
Station
station_level
```

后续如果需要 `StationLevel` 这种别名，再扩展枚举解析。

当前实现支持的标准值是：

```text
step
run
station
```

## 6. 变量替换

Station config 复用 `VariableResolver`。

支持：

```text
${PROJECT_DIR}
${SEQUENCE_DIR}
显式 variables
环境变量
```

示例：

```json
{
  "address": "${DMM1_ADDRESS}"
}
```

测试里通过：

```cpp
VariableResolverOptions options;
options.variables.insert("DMM1_ADDRESS", "USB0::...");
```

传入解析器。

## 7. 当前代码入口

核心 API：

```cpp
StationConfigResult loadStationConfigFile(
    const QString& filePath,
    VariableResolverOptions resolverOptions = {});

StationConfigResult parseStationConfigJson(
    const QJsonObject& object,
    const VariableResolverOptions& resolverOptions = {});

QVector<StationConfigDiagnostic> configureDeviceSessions(
    const StationConfig& config,
    DeviceSessionManager& manager);
```

运行期统一入口：

```cpp
class StationRuntime {
public:
    StationRuntimeResult loadStationConfigFile(
        const QString& filePath,
        VariableResolverOptions resolverOptions = {});

    StationRuntimeResult applyStationConfig(const StationConfig& config);

    bool hasStationConfig() const;
    const StationConfig& stationConfig() const;

    DeviceSessionManager& devices();
    const DeviceSessionManager& devices() const;
};
```

位置：

```text
src/core/include/PicoATE/Core/StationConfig.h
src/core/src/StationConfig.cpp
src/core/include/PicoATE/Core/StationRuntime.h
src/core/src/StationRuntime.cpp
```

## 8. 当前已经完成

已经完成：

- station config JSON 解析
- `devices[]` 类型校验
- `deviceId/id`、`deviceType/type`、`driverId/driver`、`address/visaAddress` 别名
- `enabled: false` 跳过设备
- `lifetime` 解析
- 重复 `deviceId` 报错
- 变量替换
- `options` 保留结构化配置
- `configureDeviceSessions()` 将 station config 写入 `DeviceSessionManager`
- `StationRuntime` 统一持有 station config 和 `DeviceSessionManager`
- CLI 支持 `--station`
- CLI 会为 `fake.dmm` / `fake.can` 注册 fake instrument device session factory
- 业务模块可通过 `ModuleExecutionContext.runtimeServices` 使用 station 中的逻辑设备
- `examples/dmm_can_adapter_sequence.json` 已验证 `DMM1/CAN1` 从 station 到 adapter 再到 fake host 的链路
- 示例文件
- 单元测试
- CLI 示例测试

## 9. 当前还没完成

还没完成：

- `ExecutionSession` 自动接收 station config
- 真实 DMM/CAN/PSU driver
- 生产级 Persistent Instrument Host 诊断、版本协商、能力查询
- UI 工站配置页面

也就是说：

```text
Station config 现在能解析、能校验、能配置 DeviceSessionManager，
也可以通过 CLI --station 在运行 sequence 前加载并打印设备摘要。

业务模块现在已经可以通过 runtime services 使用逻辑设备。

但它还没有验证真实硬件，也还没有生产级设备状态/诊断 UI。
```

## 10. 推荐下一步

下一步建议：

| 顺序 | 任务 | 说明 |
|------|------|------|
| 1 | UI 工站配置页面 | 后续在 Qt UI 里显示 station、device 状态和诊断。 |
| 2 | Host/Transport 诊断增强 | 接真实硬件前补清楚 timeout、stderr、协议错误。 |
| 3 | 真实 DMM/CAN/PSU driver | 用项目 adapter 替换 fake host。 |
| 4 | ExecutionSession / StationRuntime 合并入口 | 后续 UI 可以只持有一个统一运行上下文。 |

## 11. CLI 用法

命令行可以在运行 sequence 前加载 station config：

```powershell
$env:DMM1_ADDRESS="USB0::0x0957::0x0607::MY59001234::INSTR"
.\out\build\vs2022-qt6\src\cli\Debug\PicoATE.Cli.exe run examples\simple_sequence.json --station examples\stations\basic_station.json
.\out\build\vs2022-qt6\src\cli\Debug\PicoATE.Cli.exe run examples\dmm_can_adapter_sequence.json --station examples\stations\basic_station.json
```

当前输出会包含：

```text
Station: PicoATE Bench 01 [bench-01]
Devices: 2 configured
  - DMM1 [DMM] fake.dmm lifetime=Station address=USB0::...
  - CAN1 [CAN] fake.can lifetime=Run address=...
```

## 逻辑设备与插件绑定（2026-07-12）

测试流程不应直接写 GCAN、ZLG 等厂家插件 ID。Sequence 只引用稳定的逻辑设备：

```json
{
  "moduleId": "device",
  "function": "write",
  "inputs": {
    "deviceId": "CAN1.CH1",
    "id": "0x123",
    "data": "01 02 03 04"
  }
}
```

`StationSystem.json` 负责决定 `CAN1` 当前使用哪个插件：

```json
{
  "deviceId": "CAN1.CH1",
  "deviceType": "CAN",
  "physicalDeviceId": "CAN1",
  "channelId": "CH1",
  "driverId": "plugin.can.gcan",
  "pluginPath": "${PROJECT_DIR}/templates/bin/Debug/PicoATE.CAN.GCAN.dll",
  "timeoutMs": 6000,
  "lifetime": "Run",
  "enabled": true,
  "options": {
    "deviceType": 0,
    "deviceIndex": 0,
    "channelIndex": 0,
    "bitrate": 500000
  }
}
```

- `driverId` 对应 DLL 的 `PicoATE_Describe` 中的 `moduleId`。
- `pluginPath` 是实际 DLL 路径，可使用 `${PROJECT_DIR}` 等变量。
- `timeoutMs` 是该设备插件单次调用的默认超时。
- `options` 是连接该型号设备时自动补充的固定参数。
- 更换 GCAN/ZLG 等型号时，优先只修改 Station 绑定；Sequence 中的 `CAN1.CH1`、`CAN1.CH2` 和业务流程保持不变。
- Flow Editor 按通道显示 `Send [CAN1.CH1]`、`Send [CAN1.CH2]` 等明确目标，拖入后写入对应的 `deviceId`。

## CAN 物理设备与通道模型（2026-07-16）

CAN 配置明确区分“物理盒子”和“可执行通道”：

```text
CAN1                 物理 CAN 盒
  CH1 -> CAN1.CH1    第 1 路运行时端点
  CH2 -> CAN1.CH2    第 2 路运行时端点
CAN2
  CH1 -> CAN2.CH1
  CH2 -> CAN2.CH2
```

约束如下：

- Devices 页面以树形展示物理设备和通道。
- `Type`、`Driver / Model`、`Lifetime`、`Enable` 在 Devices 页面直接配置。
- CAN 物理设备选择一次插件型号，型号绑定会同步到其 CH1/CH2。
- CH1/CH2 是固定通道，不能单独删除；不使用的通道直接设为 OFF。
- Sequence 只引用端点 ID，例如 `CAN1.CH2`，不会引用厂家名。
- `channelIndex` 由框架根据 CH1/CH2 自动维护，右侧参数页不允许手工修改。
- 旧配置中的 `GCAN_CAN1`、`GCAN_CAN2`、`CX_CAN1` 等标识，在 Admin 加载时会迁移为 `CANx.CH1/CH2`，并同步修改 Flow 中的引用。

Station JSON 仍保持扁平 `devices[]`，方便 Core 和现有运行时直接消费：

```json
{
  "deviceId": "CAN1.CH1",
  "deviceType": "CAN",
  "physicalDeviceId": "CAN1",
  "channelId": "CH1",
  "driverId": "plugin.can.gcan",
  "enabled": true,
  "options": {
    "deviceIndex": 0,
    "channelIndex": 0,
    "bitrate": 500000
  }
}
```

## Admin 参数编辑规则（2026-07-16）

右侧 Device Parameters 只保留当前设备的基础连接参数：

- Address 和 Timeout 属于通用参数。
- 插件 `open/connect` 功能的 `PicoATE_Describe.inputs` 用于生成型号参数。
- `enum` 显示为下拉框。
- `boolean` 显示为 OFF/ON 开关。
- `integer/number` 显示为数值控件，并应用 minimum/maximum。
- 只有无法预定义的字符串或十六进制数据才使用文本框。
- `deviceId`、`channelIndex`、`libraryPath` 等框架字段不会出现在右侧。
- 修改只进入文档草稿，最终仍以 `Ctrl+S` 保存到 `StationSystem.json`。

## 待实现：Station 设备连通性验证

设备连接测试不新增专用插件 ABI，也不让 Station UI 直接调用厂家 DLL。每个驱动提供一条普通
Sequence，统一放在工具目录的 `probe/` 下，并以 `driverId` 对应：

```text
probe/
  plugin.can.gcan.probe.json
  plugin.can.cx.probe.json
  plugin.dmm.keysight34465a.probe.json
```

Probe 仍使用现有 Action、逻辑设备和 Cleanup。例如 GCAN 可以执行
`open -> getInfo -> close`，DMM 可以执行 `connect -> getIdentity -> disconnect`。
不同厂家如何验证由各自 Probe Sequence 决定，UI 不猜测函数名称。

计划中的 Station 工作流：

1. 工程师选择 Driver / Model 并填写当前设备参数。
2. 点击 `Test Connection`，UI 使用当前表单草稿生成临时 Station 快照，不要求先保存正式文件。
3. UI 将目标 Logical ID 写入临时 Probe Sequence。
4. 通过 `QProcess` 启动同目录的 `PicoATE.Cli.exe` 执行 Probe。
5. CLI 日志实时进入 Station 诊断区，结束后由退出码和机器可读 ExecutionReport 判断结果。
6. 临时文件在结束后删除；正式 `StationSystem.json` 仍只由 `Ctrl+S` 保存。

Probe 成功表示驱动、配置和设备打开链路通过，不等同于 CAN 总线接线或业务通信通过。
发送/接收、回环、Modbus 寄存器读取等属于可选的功能诊断 Probe，应与普通连接验证分开。
CLI 后续需要提供稳定的机器可读报告参数，例如 `--report-json <path>`，UI 不解析彩色终端文字。
