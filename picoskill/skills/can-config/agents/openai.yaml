interface:
  display_name: "PicoATE CAN 脚本配置"
  short_description: "配置与校验 CAN 测试脚本"
  default_prompt: "Use $can-config to review or create a PicoATE CAN sequence against the plugin Describe and Station binding."
policy:
  allow_implicit_invocation: true
